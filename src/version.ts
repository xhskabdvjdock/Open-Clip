/** Single source of truth for the app version shown in the UI.
 *  Keep in sync with package.json, src-tauri/tauri.conf.json and
 *  src-tauri/Cargo.toml when releasing a new version. */
export const APP_VERSION = "1.0.0";
